 letExp(
  decList(
   varDec(i,
    int,
    intExp(5),
    TRUE),
   decList(
    varDec(a,
     a,
     intExp(0),
     TRUE),
    decList())),
  seqExp(
   expList(
    letExp(
     decList(
      varDec(i,
       int,
       intExp(5),
       TRUE),
      decList(
       varDec(a,
        a,
        intExp(0),
        TRUE),
       decList())),
     seqExp(
      expList(
       varExp(
        simpleVar(i)),
       expList()))),
    expList())))